using System;
// using imports a namespace so you can use its classes without writing their full names.
// A namespace groups related classes together.
public class HelloWorld
{
public void Hello()
    {
       Console.WriteLine("Hello World!");
       Console.WriteLine("Hey, I'm Shivani Here!");
    // The Console class is inside the System namespace.
    // A built-in class used for interacting with the console.
    // WriteLine
    // A method that prints text to the console and then moves the cursor to the next line.
    }
}